---
type: Operation
title: (callable name, or family name for a group)
description: "(one sentence: what calling this does)"
tags: [(api area), (family?)]
aliases: [(EVERY member name if this is a family page — search must reach it
  by any member; plus natural-language phrasings and cross-language aliases)]
sources: [(corpus-relative paths: header, doc page)]
---
## Signature

(The REAL signature(s), copied, not paraphrased. For a family: a table of
member → signature. For an endpoint: method, path, auth.)

## Parameters

(Each parameter: meaning, valid range/values, ownership if applicable.)

## Returns

(Shape and meaning of the result; link the [Type](/types/x.md) it produces.
Who owns/frees it if the API has ownership rules — link the Contract.)

## Errors

(Enumerated: what can fail, how failure is signalled, what the caller must
check. "Returns NULL on error" alone is not enough — say when.)

## Example

(A minimal RUNNABLE call in the API's language. Real names, real types.)
